<script>
  new Graph({
    target  : '#<%= outcome %>-<%= @school.facility_code %>',
    title   : "Salary Rates",
    columns : { all : '<%= vrate %>}' },
    average : '<%= nrate %>' 
  });
</script>
